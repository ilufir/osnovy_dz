#include <stdio.h>
#include <math.h>

int main()
{
    int a;
    scanf("%d",&a);
    if ((a%10 == 3) || (a/10 == 3)){
        (printf("ECTb 3"));
    }
    else{
        (printf("HET 3"));
    }
    return 0;
}
